# Welcome to your Lovable project

## Local email setup

Create a `.env.local` file in the project root with:

```env
RESEND_API_KEY=your_resend_api_key
RESEND_FROM_EMAIL=Fincile <onboarding@resend.dev>
```

For production, add the same variables in your hosting or Lovable environment settings.
